[tool.poetry]
name = "django-project"
version = "0.1.0"
description = ""
authors = ["tkachenko <sevaatmail@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
django = "^5.2.8"
psycopg2-binary = "^2.9.11"
pillow = "^12.0.0"
ipython = "^9.7.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
